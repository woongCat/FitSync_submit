import {
    Text,
    View,
} from 'react-native';

const MenuScreen : React.FC = () => {
    return (
        <View>
            <Text>Menu Screen</Text>
        </View>
    );
};

export default MenuScreen;